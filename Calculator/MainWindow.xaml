<Window x:Class="Calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Calculator" Height="600" Width="400"
        materialDesign:ColorZoneAssist.Mode="PrimaryDark">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- 表达式显示（小字号） -->
        <TextBox x:Name="ExpressionTextBox" Grid.Row="0" FontSize="18" Foreground="Gray" Margin="0,0,0,4" TextAlignment="Right" IsReadOnly="True" BorderThickness="1" BorderBrush="Gray" Background="White" IsHitTestVisible="True"/>
        <!-- 结果显示（大字号） -->
        <TextBox x:Name="ResultTextBox" Grid.Row="1" FontSize="32" FontWeight="Bold" Foreground="Black" Margin="0,0,0,16" TextAlignment="Right" IsReadOnly="True" BorderThickness="1" BorderBrush="Gray" Background="White" IsHitTestVisible="True"/>
        <!-- 按钮区 -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <!-- 普通按钮样式 -->
                <Style x:Key="SquareAccentButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                    <Setter Property="Foreground" Value="{DynamicResource MaterialDesignAccentBrush}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignAccentBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Width" Value="60"/>
                    <Setter Property="Height" Value="60"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Margin" Value="6"/>
                </Style>
                <!-- 删除/清除按钮样式（红色，更长） -->
                <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                    <Setter Property="Foreground" Value="Red"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Width" Value="128"/>
                    <Setter Property="Height" Value="60"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Margin" Value="6"/>
                </Style>
                <!-- 等号按钮样式（蓝色文字） -->
                <Style x:Key="EqualButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                    <Setter Property="Foreground" Value="Blue"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Blue"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Width" Value="60"/>
                    <Setter Property="Height" Value="60"/>
                    <Setter Property="FontSize" Value="20"/>
                    <Setter Property="Margin" Value="6"/>
                </Style>
            </Grid.Resources>
            <!-- 第一行：DEL和CLEAR（各占两列，填满整行） -->
            <Button Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Content="DEL" Style="{StaticResource DeleteButtonStyle}" Command="{Binding DeleteCommand}"/>
            <Button Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Content="CLEAR" Style="{StaticResource DeleteButtonStyle}" Command="{Binding ClearCommand}"/>
            <!-- 第二行：7 8 9 除 -->
            <Button Grid.Row="1" Grid.Column="0" Content="7" Style="{StaticResource SquareAccentButton}" CommandParameter="7" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="1" Grid.Column="1" Content="8" Style="{StaticResource SquareAccentButton}" CommandParameter="8" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="1" Grid.Column="2" Content="9" Style="{StaticResource SquareAccentButton}" CommandParameter="9" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="1" Grid.Column="3" Content="÷" Style="{StaticResource SquareAccentButton}" Command="{Binding DivideCommand}"/>
            <!-- 第三行：4 5 6 乘 -->
            <Button Grid.Row="2" Grid.Column="0" Content="4" Style="{StaticResource SquareAccentButton}" CommandParameter="4" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="2" Grid.Column="1" Content="5" Style="{StaticResource SquareAccentButton}" CommandParameter="5" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="2" Grid.Column="2" Content="6" Style="{StaticResource SquareAccentButton}" CommandParameter="6" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="2" Grid.Column="3" Content="×" Style="{StaticResource SquareAccentButton}" Command="{Binding MultiplyCommand}"/>
            <!-- 第四行：1 2 3 减 -->
            <Button Grid.Row="3" Grid.Column="0" Content="1" Style="{StaticResource SquareAccentButton}" CommandParameter="1" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="3" Grid.Column="1" Content="2" Style="{StaticResource SquareAccentButton}" CommandParameter="2" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="3" Grid.Column="2" Content="3" Style="{StaticResource SquareAccentButton}" CommandParameter="3" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="3" Grid.Column="3" Content="-" Style="{StaticResource SquareAccentButton}" Command="{Binding SubtractCommand}"/>
            <!-- 第五行：0和.并排，=和+竖排最右侧 -->
            <Button Grid.Row="4" Grid.Column="0" Content="0" Style="{StaticResource SquareAccentButton}" CommandParameter="0" Command="{Binding NumberCommand}"/>
            <Button Grid.Row="4" Grid.Column="1" Content="." Style="{StaticResource SquareAccentButton}" CommandParameter="." Command="{Binding NumberCommand}"/>
            <Button Grid.Row="4" Grid.Column="2" Content="=" Style="{StaticResource EqualButtonStyle}" Command="{Binding EqualCommand}"/>
            <Button Grid.Row="4" Grid.Column="3" Content="+" Style="{StaticResource SquareAccentButton}" Command="{Binding AddCommand}"/>
        </Grid>
        <!-- 底部切换区 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <!-- 除法模式切换 -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                <TextBlock Text="除法模式:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ComboBox x:Name="DivisionModeCombo" Width="100" SelectedIndex="0">
                    <ComboBoxItem Content="普通除法"/>
                    <ComboBoxItem Content="带余除法"/>
                </ComboBox>
            </StackPanel>
            <!-- 结果显示模式切换 -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="结果显示:" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <ComboBox x:Name="ResultModeCombo" Width="100" SelectedIndex="0">
                    <ComboBoxItem Content="小数"/>
                    <ComboBoxItem Content="分数"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
